# TASK 8 — Menu category filter

`GET /menu-items` accepts optional query `category` with values `drink` or `food`. When set, return only active items in that category. When omitted, behavior stays the same (all active items). Invalid `category` values return 400. Prior tasks must still pass.
