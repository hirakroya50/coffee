# TASK 12 — Ping

Add `GET /ping` that returns HTTP 200 and JSON `{ "ping": "pong" }`. Document the route in OpenAPI with a unique `operationId`. Do not change existing endpoints or harness logic. Prior tasks must still pass.
