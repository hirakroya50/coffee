# TASK 15 — Readiness

Add `GET /ready` that returns HTTP 200 and JSON `{ "ready": true }`. Document the route in OpenAPI with a unique `operationId`. Do not change existing endpoints or harness logic. Prior tasks must still pass.
