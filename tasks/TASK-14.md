# TASK 14 — Service info

Add `GET /info` that returns HTTP 200 and JSON `{ "name": "coffee-shop", "api": "v1" }`. Document the route in OpenAPI with a unique `operationId`. Do not change existing endpoints or harness logic. Prior tasks must still pass.
