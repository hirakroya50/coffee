# TASK 1 — Drink sizes

Add drink sizes SMALL, MEDIUM, and LARGE for drinks. Medium adds $0.50 (50 cents). Large adds $1.00 (100 cents). Food must not include a size. The server computes the final unit price from SQLite/Postgres menu prices plus the size adjustment. Keep OpenAPI, schema, code, and tests synchronized.
