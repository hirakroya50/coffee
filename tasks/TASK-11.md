# TASK 11 — Health check

Add `GET /health` that returns HTTP 200 and JSON `{ "status": "ok", "service": "coffee-shop" }`. Document the route in OpenAPI with a unique `operationId`. Do not change existing endpoints or harness logic. Prior tasks must still pass.
