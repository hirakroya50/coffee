# TASK 4 — Order notes

Orders may include optional `notes` (special instructions). Max length 200 characters. Reject empty strings and values over 200 with 400. Store on the order and return `notes` on create and when fetching the order. Prior size, milk, and cancellation behavior must still pass.
