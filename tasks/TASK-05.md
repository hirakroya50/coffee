# TASK 5 — Extra shot

Drink line items may set `extra_shot: true` to add $0.75 (75 cents) per unit to the server-computed unit price. Food must reject `extra_shot`. Drinks without the flag behave as today. Keep OpenAPI, schema, code, and tests synchronized. Prior tasks must still pass.
