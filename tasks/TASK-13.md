# TASK 13 — Version

Add `GET /version` that returns HTTP 200 and JSON `{ "version": "0.0.0" }`. Document the route in OpenAPI with a unique `operationId`. Do not change existing endpoints or harness logic. Prior tasks must still pass.
